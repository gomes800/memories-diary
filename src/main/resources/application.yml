spring:
  applicaton:
    name:
      memories-diary
  datasource:
    url: jdbc:postgresql://localhost:5432/memories_diary
    username: postgres
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

api:
  security:
    token:
      secret: dev-secret
      expiration: 3600000